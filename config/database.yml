default: &default
  adapter: postgis
  encoding: unicode
  pool: <%= ENV.fetch("DB_POOL") { 5 } %>
  timeout: 5000
  host: postgres
  username: postgres
  password:

development:
  <<: *default
  url: app_development

test:
  <<: *default
  url: <%= ENV.fetch('TEST_DATABASE_URL', 'app_test') %>

production:
  adapter: postgis
  encoding: unicode
  pool: <%= ENV.fetch("DB_POOL") { 5 } %>
  timeout: 5000
  url: app_production
